extends layout

block extraHeader
  title D&D Platform - Login

block content
  include _login
  include _signup

  script.
    if ('#{error}') {
      alertify.error("#{error}");
    }
    if ('#{success}') {
      alertify.success('#{success}');
    }
